<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - SIPANDI</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="icon" type="image/png" href="/images/logo-biro-organisasi.jpg">
</head>
<body class="bg-gray-100 font-sans antialiased">
    <% if (!pageTitle) { %>
        <% const _map = { 'admin-barang': 'Manajemen Barang', 'admin-riwayat-pengajuan': 'Riwayat Pengajuan', 'admin-riwayat-jp': 'Riwayat JP', 'admin-users': 'Manajemen Pengguna' }; %>
        <% pageTitle = _map[page] || (page ? page.replace(/[-_]/g, ' ') : 'Placeholder'); %>
    <% } %>

    <% var sidebarPartial = (user && user.role === 'admin') ? 'partials/sidebaradmin' : 'partials/sidebar'; %>

    <div class="flex h-screen overflow-hidden">
        
        <%- include(sidebarPartial, { page: page }) %>

        <div class="relative flex flex-col flex-1 overflow-y-auto overflow-x-hidden">
            
            <%- include('partials/header', { pageTitle: pageTitle }) %>

            <main class="w-full flex-grow p-6 flex items-center justify-center">
                <div class="text-center p-8 bg-white rounded-2xl shadow-sm border border-gray-100 max-w-lg w-full">
                    <div class="inline-flex items-center justify-center w-20 h-20 mb-6 rounded-full bg-sipandi-green-50 text-sipandi-green-600">
                        <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.384-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path>
                        </svg>
                    </div>
                    <h2 class="text-2xl font-bold text-gray-800 mb-3">Halaman Masih Kosong</h2>
                    <p class="text-gray-500 mb-8">
                        Fitur <strong><%= pageTitle %></strong> saat ini belum tersedia. Silakan kembali lagi nanti atau hubungi administrator jika Anda memerlukan bantuan.
                    </p>
                    <button onclick="history.back()" class="inline-flex items-center px-6 py-3 bg-sipandi-green-600 hover:bg-sipandi-green-700 text-white font-medium rounded-xl transition duration-200">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                        Kembali
                    </button>
                </div>
            </main>

            <%- include('partials/footer') %>
            
        </div>
    </div>
</body>
</html>