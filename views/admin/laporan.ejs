<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pusat Laporan - SIPANDI</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="icon" type="image/png" href="/images/logo-biro-organisasi.jpg">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        @media print {
            body * { visibility: hidden; }
            .printable-area, .printable-area * { visibility: visible; }
            .printable-area {
                position: absolute;
                left: 0; top: 0; width: 100%;
                background: white !important;
            }
            .no-print { display: none !important; }
            table { width: 100% !important; border-collapse: collapse; }
            th, td { 
                word-break: break-all !important; 
                white-space: normal !important; 
                border: 1px solid #e5e7eb !important;
                padding: 8px !important;
            }
            .stats-container { display: none !important; }
        }
        .report-section {
            animation: fadeIn 0.3s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        /* Custom class untuk kartu aktif */
        .filter-card-active {
            transform: translateY(-4px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            border-color: #22c55e; /* sipandi-green */
            background-color: #f0fdf4; /* green-50 */
        }
    </style>
</head>

<body class="bg-gray-50 font-sans antialiased">
    <div class="flex h-screen overflow-hidden">
        <%- include('../partials/sidebaradmin', { page: 'laporan' }) %>
        
        <div class="relative flex flex-col flex-1 overflow-y-auto overflow-x-hidden">
            <%- include('../partials/header', { pageTitle: 'Pusat Laporan & Arsip' }) %>

            <main class="w-full flex-grow p-6">
                <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center mb-8 gap-4 no-print">
                    <div class="bg-white p-2 rounded-2xl shadow-sm border border-gray-100 inline-flex flex-wrap gap-2">
                        <% if (user.role === 'admin' || user.role === 'admin_atk') { %>
                            <button onclick="switchTab('pengajuan')" id="tab-pengajuan" class="tab-btn px-6 py-2.5 rounded-xl font-bold text-sm transition-all bg-sipandi-green-600 text-white shadow-md shadow-green-100">
                                💼 Pengajuan Barang
                            </button>
                            <button onclick="switchTab('stok')" id="tab-stok" class="tab-btn px-6 py-2.5 rounded-xl font-bold text-sm transition-all text-gray-500 hover:bg-gray-50">
                                📦 Stok Barang
                            </button>
                        <% } %>

                        <% if (user.role === 'admin' || user.role === 'admin_validasi_jp') { %>
                            <button onclick="switchTab('jp')" id="tab-jp" class="tab-btn px-6 py-2.5 rounded-xl font-bold text-sm transition-all text-gray-500 hover:bg-gray-50">
                                🎓 Validasi JP
                            </button>
                        <% } %>

                        <% if (user.role === 'admin') { %>
                            <button onclick="switchTab('users')" id="tab-users" class="tab-btn px-6 py-2.5 rounded-xl font-bold text-sm transition-all text-gray-500 hover:bg-gray-50">
                                👥 Data Pegawai
                            </button>
                        <% } %>
                    </div>

                    <div class="flex items-center gap-2 bg-white p-2 rounded-2xl shadow-sm border border-gray-100">
                        <select id="filterBulan" onchange="applyFilters()" class="text-sm border-none focus:ring-0 bg-transparent font-bold text-gray-700 cursor-pointer">
                            <option value="all">Semua Bulan</option>
                            <option value="0">Januari</option>
                            <option value="1">Februari</option>
                            <option value="2">Maret</option>
                            <option value="3">April</option>
                            <option value="4">Mei</option>
                            <option value="5">Juni</option>
                            <option value="6">Juli</option>
                            <option value="7">Agustus</option>
                            <option value="8">September</option>
                            <option value="9">Oktober</option>
                            <option value="10">November</option>
                            <option value="11">Desember</option>
                        </select>
                        <div class="h-6 w-px bg-gray-200"></div>
                        <select id="filterTahun" onchange="applyFilters()" class="text-sm border-none focus:ring-0 bg-transparent font-bold text-gray-700 cursor-pointer">
                            <option value="all">Semua Tahun</option>
                            <% const currentYear = new Date().getFullYear(); 
                               for(let i = currentYear; i >= 2024; i--) { %>
                                <option value="<%= i %>"><%= i %></option>
                            <% } %>
                        </select>
                    </div>
                </div>

                <div id="content-pengajuan" class="report-section">
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6 stats-container no-print">
                        <div onclick="filterStatus('all')" id="card-all" class="bg-white p-4 rounded-2xl border border-gray-100 shadow-sm cursor-pointer transition-all hover:shadow-md hover:bg-gray-50 filter-card-active group">
                            <div class="flex items-center justify-between">
                                <div>
                                    <p class="text-xs font-bold text-gray-400 uppercase group-hover:text-gray-600">Total Pengajuan</p>
                                    <p id="stat-total-pengajuan" class="text-2xl font-black text-gray-800 mt-1">0</p>
                                </div>
                                <div class="p-2 bg-gray-100 rounded-lg text-gray-500">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path></svg>
                                </div>
                            </div>
                        </div>

                        <div onclick="filterStatus('disetujui')" id="card-disetujui" class="bg-white p-4 rounded-2xl border border-gray-100 shadow-sm cursor-pointer transition-all hover:shadow-md hover:bg-green-50 group border-l-4 border-l-green-500">
                            <div class="flex items-center justify-between">
                                <div>
                                    <p class="text-xs font-bold text-gray-400 uppercase group-hover:text-green-600">Disetujui</p>
                                    <p id="stat-acc-pengajuan" class="text-2xl font-black text-green-600 mt-1">0</p>
                                </div>
                                <div class="p-2 bg-green-50 rounded-lg text-green-600">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                </div>
                            </div>
                        </div>

                        <div onclick="filterStatus('diajukan')" id="card-diajukan" class="bg-white p-4 rounded-2xl border border-gray-100 shadow-sm cursor-pointer transition-all hover:shadow-md hover:bg-blue-50 group border-l-4 border-l-blue-500">
                            <div class="flex items-center justify-between">
                                <div>
                                    <p class="text-xs font-bold text-gray-400 uppercase group-hover:text-blue-600">Diajukan</p>
                                    <p id="stat-pending-pengajuan" class="text-2xl font-black text-blue-600 mt-1">0</p>
                                </div>
                                <div class="p-2 bg-blue-50 rounded-lg text-blue-600">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                                </div>
                            </div>
                        </div>

                        <div onclick="filterStatus('ditolak')" id="card-ditolak" class="bg-white p-4 rounded-2xl border border-gray-100 shadow-sm cursor-pointer transition-all hover:shadow-md hover:bg-red-50 group border-l-4 border-l-red-500">
                            <div class="flex items-center justify-between">
                                <div>
                                    <p class="text-xs font-bold text-gray-400 uppercase group-hover:text-red-600">Ditolak</p>
                                    <p id="stat-rejected-pengajuan" class="text-2xl font-black text-red-600 mt-1">0</p>
                                </div>
                                <div class="p-2 bg-red-50 rounded-lg text-red-600">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden printable-area">
                        <div class="p-8 border-b border-gray-50 flex flex-col md:flex-row justify-between items-center gap-4">
                            <div>
                                <h3 class="text-xl font-black text-gray-800 uppercase tracking-tight">Laporan Pengajuan Barang</h3>
                                <p id="period-text-pengajuan" class="text-sm text-gray-500 italic">Periode: Semua Data</p>
                            </div>
                            <div class="flex gap-2 no-print">
                                <button onclick="exportToExcel('tablePengajuan', 'Laporan_Pengajuan')" class="p-2.5 bg-green-50 text-green-700 rounded-xl hover:bg-green-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                                <button onclick="printArea('content-pengajuan')" class="p-2.5 bg-blue-50 text-blue-700 rounded-xl hover:bg-blue-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                            </div>
                        </div>
                        <div class="overflow-x-auto">
                            <table id="tablePengajuan" class="w-full text-sm">
                                <thead class="bg-gray-50/50 text-gray-500 font-bold uppercase text-[10px] tracking-widest border-b border-gray-100">
                                    <tr>
                                        <th class="px-8 py-4 text-left">Tanggal</th>
                                        <th class="px-8 py-4 text-left">Pegawai</th>
                                        <th class="px-8 py-4 text-left">Barang</th>
                                        <th class="px-8 py-4 text-left">Status</th>
                                        <th class="px-8 py-4 text-left">Catatan Admin</th>
                                    </tr>
                                </thead>
                                <tbody id="bodyPengajuan" class="divide-y divide-gray-50"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <div id="content-stok" class="report-section hidden">
                    <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden printable-area">
                        <div class="p-8 border-b border-gray-50 flex flex-col md:flex-row justify-between items-center gap-4">
                            <div>
                                <h3 class="text-xl font-black text-gray-800 uppercase tracking-tight">Laporan Inventaris Barang</h3>
                                <p class="text-sm text-gray-500">Update Stok Realtime Biro Organisasi</p>
                            </div>
                            <div class="flex gap-2 no-print">
                                <button onclick="exportToExcel('tableStok', 'Laporan_Stok')" class="p-2.5 bg-green-50 text-green-700 rounded-xl hover:bg-green-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                                <button onclick="printArea('content-stok')" class="p-2.5 bg-blue-50 text-blue-700 rounded-xl hover:bg-blue-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                            </div>
                        </div>
                        <table id="tableStok" class="w-full text-sm">
                            <thead class="bg-gray-50/50 text-gray-500 font-bold uppercase text-[10px] tracking-widest border-b border-gray-100">
                                <tr>
                                    <th class="px-8 py-4 text-left">Nama Barang</th>
                                    <th class="px-8 py-4 text-left">Stok</th>
                                    <th class="px-8 py-4 text-left">Satuan</th>
                                    <th class="px-8 py-4 text-left">Status</th>
                                    <th class="px-8 py-4 text-left">Terakhir Update</th>
                                </tr>
                            </thead>
                            <tbody id="bodyStok" class="divide-y divide-gray-50"></tbody>
                        </table>
                    </div>
                </div>

                <div id="content-jp" class="report-section hidden">
                    <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden printable-area">
                        <div class="p-8 border-b border-gray-50 flex flex-col md:flex-row justify-between items-center gap-4">
                            <div>
                                <h3 class="text-xl font-black text-gray-800 uppercase tracking-tight">Rekapitulasi Jam Pelajaran ASN</h3>
                                <p id="period-text-jp" class="text-sm text-gray-500 italic">Periode: Semua Data</p>
                            </div>
                            <div class="flex gap-2 no-print">
                                <button onclick="exportToExcel('tableJp', 'Laporan_JP')" class="p-2.5 bg-green-50 text-green-700 rounded-xl hover:bg-green-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                                <button onclick="printArea('content-jp')" class="p-2.5 bg-blue-50 text-blue-700 rounded-xl hover:bg-blue-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                            </div>
                        </div>
                        <table id="tableJp" class="w-full text-sm">
                            <thead class="bg-gray-50/50 text-gray-500 font-bold uppercase text-[10px] tracking-widest border-b border-gray-100">
                                <tr>
                                    <th class="px-8 py-4 text-left">Pegawai</th>
                                    <th class="px-8 py-4 text-left">Sertifikat</th>
                                    <th class="px-8 py-4 text-center">JP</th>
                                    <th class="px-8 py-4 text-left">Periode</th>
                                    <th class="px-8 py-4 text-left">Status</th>
                                </tr>
                            </thead>
                            <tbody id="bodyJp" class="divide-y divide-gray-50"></tbody>
                        </table>
                    </div>
                </div>

                <div id="content-users" class="report-section hidden">
                    <div class="bg-white rounded-3xl shadow-sm border border-gray-100 overflow-hidden printable-area">
                        <div class="p-8 border-b border-gray-50 flex flex-col md:flex-row justify-between items-center gap-4">
                            <div>
                                <h3 class="text-xl font-black text-gray-800 uppercase tracking-tight">Database Pegawai Sistem</h3>
                                <p class="text-sm text-gray-500">Daftar Akun Aktif SIPANDI</p>
                            </div>
                            <div class="flex gap-2 no-print">
                                <button onclick="exportToExcel('tableUsers', 'Data_Pegawai')" class="p-2.5 bg-green-50 text-green-700 rounded-xl hover:bg-green-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                                <button onclick="printArea('content-users')" class="p-2.5 bg-blue-50 text-blue-700 rounded-xl hover:bg-blue-100 transition-colors">
                                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                                </button>
                            </div>
                        </div>
                        <table id="tableUsers" class="w-full text-sm">
                            <thead class="bg-gray-50/50 text-gray-500 font-bold uppercase text-[10px] tracking-widest border-b border-gray-100">
                                <tr>
                                    <th class="px-8 py-4 text-left">Nama</th>
                                    <th class="px-8 py-4 text-left">NIP</th>
                                    <th class="px-8 py-4 text-left">Unit Kerja</th>
                                    <th class="px-8 py-4 text-left">Email</th>
                                    <th class="px-8 py-4 text-left">Role</th>
                                </tr>
                            </thead>
                            <tbody id="bodyUsers" class="divide-y divide-gray-50"></tbody>
                        </table>
                    </div>
                </div>

            </main>
            <%- include('../partials/footer') %>
        </div>
    </div>

    <script>
        // Global Data Store
        let masterData = {
            pengajuan: [],
            stok: [],
            jp: [],
            users: []
        };

        let currentStatusFilter = 'all'; // Menyimpan filter status aktif

        const MONTH_NAMES = ["Januari", "Februari", "Maret", "April", "Mei", "Juni", "Juli", "Agustus", "September", "Oktober", "November", "Desember"];

        document.addEventListener('DOMContentLoaded', () => {
            const role = "<%= user.role %>";
            fetchAllData(role);

            // Cek URL Parameter 'tab' untuk switch otomatis
            const urlParams = new URLSearchParams(window.location.search);
            const tabParam = urlParams.get('tab');
            if(tabParam) {
                switchTab(tabParam);
            }
        });

        async function fetchAllData(role) {
            if(['admin', 'admin_atk'].includes(role)) {
                await loadPengajuan();
                await loadStok();
            }
            if(['admin', 'admin_validasi_jp'].includes(role)) {
                await loadJp();
            }
            if(role === 'admin') {
                await loadUsers();
            }
            applyFilters(); // Initial render
        }

        function switchTab(tabName) {
            document.querySelectorAll('.report-section').forEach(el => el.classList.add('hidden'));
            const targetContent = document.getElementById(`content-${tabName}`);
            if(targetContent) {
                targetContent.classList.remove('hidden');
            }

            document.querySelectorAll('.tab-btn').forEach(btn => {
                btn.classList.remove('bg-sipandi-green-600', 'text-white', 'shadow-md', 'shadow-green-100');
                btn.classList.add('text-gray-500', 'hover:bg-gray-50');
            });

            const activeBtn = document.getElementById(`tab-${tabName}`);
            if(activeBtn) {
                activeBtn.classList.add('bg-sipandi-green-600', 'text-white', 'shadow-md', 'shadow-green-100');
                activeBtn.classList.remove('text-gray-500', 'hover:bg-gray-50');
            }
        }

        // --- FILTER STATUS LOGIC ---
        function filterStatus(status) {
            currentStatusFilter = status;
            
            // Visual Update: Highlight active card
            ['all', 'disetujui', 'diajukan', 'ditolak'].forEach(s => {
                const card = document.getElementById(`card-${s}`);
                if (s === status) {
                    card.classList.add('filter-card-active', 'bg-gray-50');
                } else {
                    card.classList.remove('filter-card-active', 'bg-gray-50');
                }
            });

            // Re-apply filters to table
            applyFilters();
        }

        // --- FETCH LOGIC ---
        async function loadPengajuan() {
            const res = await fetch('/admin/pengajuan/list?status=all&bidang=all');
            const data = await res.json();
            if(data.success) masterData.pengajuan = data.pengajuans;
        }

        async function loadStok() {
            const res = await fetch('/admin/inventory/list');
            const data = await res.json();
            if(data.success) masterData.stok = data.barangs;
        }

        async function loadJp() {
            const res = await fetch('/admin/jp/list?status=all&bidang=all&bulan=all&tahun=all');
            const data = await res.json();
            if(data.success) masterData.jp = data.submissions;
        }

        async function loadUsers() {
            const res = await fetch('/admin/users/list?role=all');
            const data = await res.json();
            if(data.success) masterData.users = data.users;
        }

        // --- FILTER ENGINE ---
        function applyFilters() {
            const selectedMonth = document.getElementById('filterBulan').value; 
            const selectedYear = document.getElementById('filterTahun').value;

            renderFilteredPengajuan(selectedMonth, selectedYear);
            renderFilteredJp(selectedMonth, selectedYear);
            renderStok(); 
            renderUsers();
            
            updatePeriodText(selectedMonth, selectedYear);
        }

        function updatePeriodText(m, y) {
            const text = (m === 'all' && y === 'all') 
                ? "Semua Data" 
                : `${m !== 'all' ? MONTH_NAMES[m] : ''} ${y !== 'all' ? y : ''}`.trim();
            
            document.getElementById('period-text-pengajuan').innerText = `Periode: ${text}`;
            document.getElementById('period-text-jp').innerText = `Periode: ${text}`;
        }

        function renderFilteredPengajuan(m, y) {
            const tbody = document.getElementById('bodyPengajuan');
            
            // Calculate Total Stats (Before Filtering by Status, But After Date)
            // Ini agar angka di kartu tetap menunjukkan total berdasarkan periode, bukan 0 jika di filter
            const dateFiltered = masterData.pengajuan.filter(p => {
                const date = new Date(p.tanggal_pengajuan);
                const matchM = m === 'all' || date.getMonth().toString() === m;
                const matchY = y === 'all' || date.getFullYear().toString() === y;
                return matchM && matchY;
            });

            // Update Stats Card Numbers
            document.getElementById('stat-total-pengajuan').innerText = dateFiltered.length;
            document.getElementById('stat-acc-pengajuan').innerText = dateFiltered.filter(p => p.status === 'disetujui').length;
            document.getElementById('stat-pending-pengajuan').innerText = dateFiltered.filter(p => p.status === 'diajukan').length;
            document.getElementById('stat-rejected-pengajuan').innerText = dateFiltered.filter(p => p.status === 'ditolak').length;

            // Apply Status Filter for Table Display
            const finalFiltered = dateFiltered.filter(p => {
                if (currentStatusFilter === 'all') return true;
                return p.status === currentStatusFilter;
            });

            tbody.innerHTML = finalFiltered.map(p => {
                let statusClass = p.status === 'disetujui' ? "bg-green-100 text-green-700" : (p.status === 'ditolak' ? "bg-red-100 text-red-700" : "bg-yellow-100 text-yellow-700");
                return `
                    <tr class="hover:bg-gray-50 transition-colors">
                        <td class="px-8 py-4 font-medium text-gray-500">${new Date(p.tanggal_pengajuan).toLocaleDateString('id-ID')}</td>
                        <td class="px-8 py-4"><span class="font-bold text-gray-800">${p.user.nama}</span><br><span class="text-[10px] text-gray-400 font-mono">${p.user.nip}</span></td>
                        <td class="px-8 py-4 text-xs">
                            ${p.items.map(i => `<div class="mb-1">• ${i.nama_barang} <b>(${i.jumlah_diminta} ${i.satuan}${p.status === 'disetujui' ? ' ➔ ' + i.jumlah_disetujui : ''})</b></div>`).join('')}
                        </td>
                        <td class="px-8 py-4"><span class="px-3 py-1 rounded-full text-[10px] font-black uppercase ${statusClass}">${p.status}</span></td>
                        <td class="px-8 py-4 text-xs italic text-gray-400">${p.catatan_admin || '-'}</td>
                    </tr>
                `;
            }).join('') || '<tr><td colspan="5" class="p-8 text-center text-gray-400">Tidak ada data untuk filter ini</td></tr>';
        }

        function renderFilteredJp(m, y) {
            const tbody = document.getElementById('bodyJp');
            const filtered = masterData.jp.filter(s => {
                const matchM = m === 'all' || s.bulan === MONTH_NAMES[parseInt(m)];
                const matchY = y === 'all' || s.tahun.toString() === y;
                return matchM && matchY;
            });

            tbody.innerHTML = filtered.map(s => {
                let jpStatusClass = s.status === 'diterima' || s.status === 'disetujui' ? "bg-green-100 text-green-700" : (s.status === 'ditolak' ? "bg-red-100 text-red-700" : "bg-yellow-100 text-yellow-700");
                return `
                    <tr class="hover:bg-gray-50 transition-colors">
                        <td class="px-8 py-4"><span class="font-bold text-gray-800">${s.user.nama}</span><br><span class="text-xs text-gray-400 font-mono">${s.user.nip}</span></td>
                        <td class="px-8 py-4 text-gray-600 font-medium">${s.nama_sertif}</td>
                        <td class="px-8 py-4 text-center font-black text-sipandi-green-600">${s.jumlah_jp}</td>
                        <td class="px-8 py-4 text-gray-500 text-xs">${s.bulan} ${s.tahun}</td>
                        <td class="px-8 py-4"><span class="px-3 py-1 rounded-full text-[10px] font-black uppercase ${jpStatusClass}">${s.status}</span></td>
                    </tr>
                `;
            }).join('') || '<tr><td colspan="5" class="p-8 text-center text-gray-400">Tidak ada data untuk periode ini</td></tr>';
        }

        function renderStok() {
            const tbody = document.getElementById('bodyStok');
            const selectedMonth = document.getElementById('filterBulan').value;
            const selectedYear = document.getElementById('filterTahun').value;

            const filtered = masterData.stok.filter(b => {
                const date = new Date(b.updatedAt);
                const matchM = selectedMonth === 'all' || date.getMonth().toString() === selectedMonth;
                const matchY = selectedYear === 'all' || date.getFullYear().toString() === selectedYear;
                return matchM && matchY;
            });

            if (filtered.length === 0) {
                tbody.innerHTML = '<tr><td colspan="5" class="p-8 text-center text-gray-400">Tidak ada aktivitas pembaruan stok untuk periode ini</td></tr>';
                return;
            }

            tbody.innerHTML = filtered.map(b => {
                let stokClass = b.status_stok === 'Stok Sedikit' ? "bg-yellow-100 text-yellow-700" : (b.status_stok === 'Perbarui Stok' || b.quantity <= 0 ? "bg-red-100 text-red-700" : "bg-green-100 text-green-700");
                return `
                    <tr class="hover:bg-gray-50 transition-colors">
                        <td class="px-8 py-4 font-bold text-gray-800">${b.nama_barang}</td>
                        <td class="px-8 py-4 font-black ${b.quantity <= 5 ? 'text-red-600' : 'text-gray-900'}">${b.quantity}</td>
                        <td class="px-8 py-4 text-gray-500">${b.satuan}</td>
                        <td class="px-8 py-4"><span class="px-3 py-1 rounded-full text-[10px] font-black uppercase ${stokClass}">${b.status_stok}</span></td>
                        <td class="px-8 py-4 text-gray-400 text-xs">${new Date(b.updatedAt).toLocaleDateString('id-ID', {day: 'numeric', month: 'long', year: 'numeric'})}</td>
                    </tr>
                `;
            }).join('');
        }

        function renderUsers() {
            const tbody = document.getElementById('bodyUsers');
            tbody.innerHTML = masterData.users.map(u => `
                <tr class="hover:bg-gray-50 transition-colors">
                    <td class="px-8 py-4 font-bold text-gray-800">${u.name}</td>
                    <td class="px-8 py-4 font-mono text-gray-500 text-xs">${u.username}</td>
                    <td class="px-8 py-4 text-gray-600">${u.unit_kerja || '-'}</td>
                    <td class="px-8 py-4 break-all text-xs font-medium text-sipandi-green-600 max-w-[200px]">${u.email || '-'}</td>
                    <td class="px-8 py-4 uppercase text-[10px] font-black text-gray-400">${u.role}</td>
                </tr>
            `).join('');
        }

        // --- UTILS ---
        function exportToExcel(tableId, filename) {
            const table = document.getElementById(tableId);
            let csv = [];
            const rows = table.querySelectorAll("tr");
            for (let i = 0; i < rows.length; i++) {
                let row = [], cols = rows[i].querySelectorAll("td, th");
                for (let j = 0; j < cols.length; j++) row.push('"' + cols[j].innerText.replace(/"/g, '""') + '"');
                csv.push(row.join(","));
            }
            const blob = new Blob([csv.join("\n")], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement("a");
            link.href = URL.createObjectURL(blob);
            link.download = `${filename}_${new Date().toLocaleDateString()}.csv`;
            link.click();
        }

        function printArea(areaId) {
            window.print();
        }
    </script>
</body>
</html>

### 2. `views/partials/sidebaradmin.ejs`
Saya memperbarui logika tautan "Laporan Rekapitulasi". Jika yang login adalah **Admin Validasi JP**, tautan akan otomatis memiliki query `?tab=jp`. Jika **Admin ATK**, maka `?tab=pengajuan`.

```ejs
<aside
    class="bg-sipandi-green-800 text-white w-64 min-h-screen flex flex-col shadow-xl transition-all duration-300 hidden md:flex">
    <div class="p-6 flex items-center space-x-3 border-b border-sipandi-green-700">
        <img src="/images/logo-biro-organisasi.jpg" alt="Logo" class="w-10 h-10 rounded-full border-2 border-white">
        <div>
            <h1 class="text-xl font-bold tracking-wider">SIPANDI</h1>
            <p class="text-xs text-sipandi-green-200">Biro Organisasi</p>
        </div>
    </div>

    <nav class="flex-1 py-6 overflow-y-auto">
        <ul class="space-y-2 px-4">

            <li>
                <% 
                    let dashboardHref = "/admin/dashboard"; // Default Admin Utama
                    if (user.role === 'admin_atk') dashboardHref = "/admin/pengajuan";
                    if (user.role === 'admin_validasi_jp') dashboardHref = "/admin/jp";
                %>
                <a href="<%= dashboardHref %>"
                    class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                    <%= (page === 'dashboard' || page === 'pengajuan' || page === 'jp') ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z">
                        </path>
                    </svg>
                    <span>Dashboard</span>
                </a>
            </li>

            <% if (user.role === 'admin' || user.role === 'admin_atk') { %>
                <li>
                    <a href="/admin/inventory"
                        class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                        <%= page === 'inventory' ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M20 7l-8-4-8 4m0 0l8 4m-8-4v10l8 4m0-10l8 4m-8-4v10l8 4M9 7l8 4"></path>
                        </svg>
                        <span>Manajemen Barang</span>
                    </a>
                </li>

                <% if (user.role === 'admin') { %>
                <li>
                    <a href="/admin/pengajuan"
                        class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                        <%= page === 'pengajuan' ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01">
                            </path>
                        </svg>
                        <span>Kelola Pengajuan</span>
                    </a>
                </li>
                <% } %>
            <% } %>

            <% if (user.role === 'admin' || user.role === 'admin_validasi_jp') { %>
                <% if (user.role === 'admin') { %>
                <li>
                    <a href="/admin/jp"
                        class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                        <%= page === 'jp' ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M9 12h6m-3-3v6m5 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z">
                            </path>
                        </svg>
                        <span>Kelola JP</span>
                    </a>
                </li>
                <% } %>
            <% } %>

            <% if (user.role === 'admin') { %>
                <li>
                    <a href="/admin/users"
                        class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                        <%= page === 'users' ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z">
                            </path>
                        </svg>
                        <span>Manajemen Pengguna</span>
                    </a>
                </li>
            <% } %>

            <li>
                <% 
                    // LOGIKA BARU: Link Laporan berdasarkan Role
                    let laporanLink = '/admin/laporan';
                    if(user.role === 'admin_validasi_jp') {
                        laporanLink += '?tab=jp';
                    } else if (user.role === 'admin_atk') {
                        laporanLink += '?tab=pengajuan';
                    }
                    // Admin utama defaultnya tetap pengajuan atau bisa diset manual
                %>
                <a href="<%= laporanLink %>"
                    class="flex items-center space-x-3 px-4 py-3 rounded-lg transition-colors duration-200 
                    <%= page === 'laporan' ? 'bg-sipandi-green-700 text-white shadow-md' : 'text-sipandi-green-100 hover:bg-sipandi-green-700 hover:text-white' %>">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                    <span>Laporan Rekapitulasi</span>
                </a>
            </li>

        </ul>
    </nav>

    <div class="p-4 border-t border-sipandi-green-700">
        <a href="#" onclick="confirmLogout(event)"
            class="flex items-center space-x-3 px-4 py-3 rounded-lg text-red-200 hover:bg-red-600 hover:text-white transition-colors duration-200">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1">
                </path>
            </svg>
            <span>Logout</span>
        </a>
    </div>
</aside>

<script src="[https://cdn.jsdelivr.net/npm/sweetalert2@11](https://cdn.jsdelivr.net/npm/sweetalert2@11)"></script>
<script>
    function confirmLogout(e) {
        e.preventDefault();
        Swal.fire({
            title: 'Yakin ingin logout?',
            text: "Anda harus login kembali untuk mengakses sistem",
            icon: 'question',
            showCancelButton: true,
            confirmButtonText: 'Ya, Logout',
            cancelButtonText: 'Batal',
            buttonsStyling: false,
            customClass: {
                popup: 'rounded-xl font-sans',
                title: 'text-lg font-bold text-gray-800',
                htmlContainer: 'text-gray-600',
                confirmButton: 'bg-red-600 hover:bg-red-700 text-white px-5 py-2.5 rounded-lg font-medium transition-colors mx-1 focus:ring-2 focus:ring-red-500 focus:ring-offset-1',
                cancelButton: 'bg-gray-200 hover:bg-gray-300 text-gray-800 px-5 py-2.5 rounded-lg font-medium transition-colors mx-1 focus:ring-2 focus:ring-gray-300 focus:ring-offset-1'
            }
        }).then((result) => {
            if (result.isConfirmed) {
                Swal.fire({
                    title: 'Logout Berhasil!',
                    text: 'Mengeluarkan anda dari sistem...',
                    icon: 'success',
                    timer: 1500,
                    showConfirmButton: false,
                    buttonsStyling: false,
                    customClass: {
                        popup: 'rounded-2xl font-sans'
                    }
                }).then(() => {
                    window.location.href = '/logout';
                });
            }
        });
    }
</script>