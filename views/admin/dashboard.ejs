<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - SIPANDI</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="icon" type="image/png" href="/images/logo-biro-organisasi.jpg">
</head>
<body class="bg-gray-50 font-sans antialiased">
    <div class="flex h-screen overflow-hidden">
        
        <%- include('../partials/sidebaradmin', { page: 'dashboard' }) %>

        <div class="relative flex flex-col flex-1 overflow-y-auto overflow-x-hidden">
            
            <%- include('../partials/header', { pageTitle: 'Dashboard Admin' }) %>

            <main class="w-full flex-grow p-6">
                
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4">
                    <div>
                        <h2 class="text-3xl font-bold text-gray-800 mb-2">Selamat Datang, <%= user.nama || user.name %>!</h2>
                        <p class="text-gray-600">Ringkasan aktivitas sistem SIPANDI hari ini.</p>
                    </div>
                    <div class="text-right hidden md:block">
                        <p class="text-sm font-semibold text-gray-500">
                            <%= new Date().toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
                        </p>
                    </div>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                    
                    <a href="/admin/pengajuan?status=diajukan" class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex items-center gap-4 hover:shadow-md hover:-translate-y-1 transition-all cursor-pointer group relative overflow-hidden">
                        <div class="absolute right-0 top-0 h-24 w-24 bg-blue-50 rounded-full -mr-10 -mt-10 transition-transform group-hover:scale-110"></div>
                        <div class="relative z-10 bg-blue-100 p-4 rounded-xl text-blue-600">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path></svg>
                        </div>
                        <div class="relative z-10">
                            <p class="text-xs font-bold text-gray-400 uppercase tracking-wider">Pengajuan Baru</p>
                            <p class="text-2xl font-black text-gray-800 mt-1"><%= stats.pendingPengajuan %></p>
                            <p class="text-xs text-blue-600 font-medium mt-1">Perlu Persetujuan</p>
                        </div>
                    </a>

                    <a href="/admin/jp?status=pending" class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex items-center gap-4 hover:shadow-md hover:-translate-y-1 transition-all cursor-pointer group relative overflow-hidden">
                        <div class="absolute right-0 top-0 h-24 w-24 bg-purple-50 rounded-full -mr-10 -mt-10 transition-transform group-hover:scale-110"></div>
                        <div class="relative z-10 bg-purple-100 p-4 rounded-xl text-purple-600">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        </div>
                        <div class="relative z-10">
                            <p class="text-xs font-bold text-gray-400 uppercase tracking-wider">Validasi JP</p>
                            <p class="text-2xl font-black text-gray-800 mt-1"><%= stats.pendingJp %></p>
                            <p class="text-xs text-purple-600 font-medium mt-1">Perlu Verifikasi</p>
                        </div>
                    </a>

                    <a href="/admin/inventory?status=stok_sedikit" class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex items-center gap-4 hover:shadow-md hover:-translate-y-1 transition-all cursor-pointer group relative overflow-hidden">
                        <div class="absolute right-0 top-0 h-24 w-24 <%= stats.lowStockItems > 0 ? 'bg-red-50' : 'bg-orange-50' %> rounded-full -mr-10 -mt-10 transition-transform group-hover:scale-110"></div>
                        <div class="relative z-10 <%= stats.lowStockItems > 0 ? 'bg-red-100 text-red-600' : 'bg-orange-100 text-orange-600' %> p-4 rounded-xl">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m0 0l8 4m-8-4v10l8 4m0-10l8 4m-8-4v10l8 4M9 7l8 4"></path></svg>
                        </div>
                        <div class="relative z-10">
                            <p class="text-xs font-bold text-gray-400 uppercase tracking-wider">Stok Menipis</p>
                            <p class="text-2xl font-black text-gray-800 mt-1"><%= stats.lowStockItems %></p>
                            <p class="text-xs <%= stats.lowStockItems > 0 ? 'text-red-600' : 'text-orange-600' %> font-medium mt-1">Perlu Restock</p>
                        </div>
                    </a>

                    <a href="/admin/users" class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex items-center gap-4 hover:shadow-md hover:-translate-y-1 transition-all cursor-pointer group relative overflow-hidden">
                        <div class="absolute right-0 top-0 h-24 w-24 bg-green-50 rounded-full -mr-10 -mt-10 transition-transform group-hover:scale-110"></div>
                        <div class="relative z-10 bg-green-100 p-4 rounded-xl text-green-600">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path></svg>
                        </div>
                        <div class="relative z-10">
                            <p class="text-xs font-bold text-gray-400 uppercase tracking-wider">Total Pegawai</p>
                            <p class="text-2xl font-black text-gray-800 mt-1"><%= stats.totalPegawai %></p>
                            <p class="text-xs text-green-600 font-medium mt-1">ASN Terdaftar</p>
                        </div>
                    </a>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">

                    <div class="card bg-white shadow-sm border border-gray-100 rounded-2xl p-6 h-full">
                        <div class="flex justify-between items-center mb-6">
                            <h3 class="text-lg font-bold text-gray-800 flex items-center gap-2">
                                <span class="bg-yellow-100 text-yellow-600 p-2 rounded-lg">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"></path></svg>
                                </span>
                                Top 3 Capaian JP
                            </h3>
                            <a href="/admin/laporan?tab=jp" class="text-sm font-bold text-yellow-600 hover:text-yellow-800 hover:underline">Lihat Laporan</a>
                        </div>
                        
                        <div class="space-y-4">
                            <% if (typeof topAsn !== 'undefined' && topAsn.length > 0) { %>
                                <% topAsn.forEach((item, index) => { %>
                                    <% 
                                        let rankClass = "bg-gray-50 border-gray-100";
                                        let medalIcon = "";
                                        if(index === 0) {
                                            rankClass = "bg-yellow-50 border-yellow-200"; 
                                            medalIcon = "🥇";
                                        } else if(index === 1) {
                                            rankClass = "bg-gray-50 border-gray-200"; 
                                            medalIcon = "🥈";
                                        } else if(index === 2) {
                                            rankClass = "bg-orange-50 border-orange-200"; 
                                            medalIcon = "🥉";
                                        }
                                    %>
                                    <div class="flex items-center justify-between p-4 rounded-xl border <%= rankClass %> transition-all hover:shadow-sm">
                                        <div class="flex items-center gap-4">
                                            <div class="flex-shrink-0 w-8 h-8 flex items-center justify-center font-bold text-lg">
                                                <%= medalIcon || (index + 1) %>
                                            </div>
                                            <div>
                                                <p class="font-bold text-gray-800 text-sm"><%= item.User.nama %></p>
                                                <p class="text-xs text-gray-500 font-mono"><%= item.User.nip %></p>
                                            </div>
                                        </div>
                                        <div class="text-right">
                                            <p class="text-lg font-black text-gray-800"><%= item.dataValues.total_jp %></p>
                                            <p class="text-[10px] uppercase font-bold text-gray-400">Total JP</p>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="text-center py-8 text-gray-400 italic bg-gray-50 rounded-xl border border-dashed border-gray-200">
                                    Belum ada data capaian JP.
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="card bg-white shadow-sm border border-gray-100 rounded-2xl p-6 h-full">
                        <div class="flex justify-between items-center mb-6">
                            <h3 class="text-lg font-bold text-gray-800 flex items-center gap-2">
                                <span class="bg-red-100 text-red-600 p-2 rounded-lg">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
                                </span>
                                Stok Menipis (Top 5)
                            </h3>
                            <a href="/admin/inventory?status=stok_sedikit" class="text-sm font-bold text-red-600 hover:text-red-800 hover:underline">Kelola Barang</a>
                        </div>
                        
                        <div class="overflow-x-auto">
                            <table class="w-full text-sm text-left">
                                <thead class="text-xs text-gray-500 uppercase bg-gray-50 border-b border-gray-100">
                                    <tr>
                                        <th class="px-4 py-3 rounded-tl-lg">Nama Barang</th>
                                        <th class="px-4 py-3 text-center">Sisa</th>
                                        <th class="px-4 py-3 text-center rounded-tr-lg">Status</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-gray-100">
                                    <% if (typeof lowStockDetails !== 'undefined' && lowStockDetails.length > 0) { %>
                                        <% lowStockDetails.forEach(barang => { %>
                                            <tr class="hover:bg-red-50/30 transition-colors">
                                                <td class="px-4 py-3 font-medium text-gray-800"><%= barang.nama_barang %></td>
                                                <td class="px-4 py-3 text-center">
                                                    <span class="font-black text-red-600"><%= barang.quantity %></span>
                                                    <span class="text-xs text-gray-400"><%= barang.satuan %></span>
                                                </td>
                                                <td class="px-4 py-3 text-center">
                                                    <% if (barang.quantity <= barang.threshold_stok_habis) { %>
                                                        <span class="px-2 py-1 bg-red-100 text-red-700 text-[10px] font-bold uppercase rounded-full">Kritis</span>
                                                    <% } else { %>
                                                        <span class="px-2 py-1 bg-orange-100 text-orange-700 text-[10px] font-bold uppercase rounded-full">Menipis</span>
                                                    <% } %>
                                                </td>
                                            </tr>
                                        <% }) %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="3" class="px-4 py-8 text-center text-gray-400 italic">
                                                Aman. Tidak ada barang dengan stok kritis.
                                            </td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>

            </main>

            <%- include('../partials/footer') %>
            
        </div>
    </div>
</body>
</html>