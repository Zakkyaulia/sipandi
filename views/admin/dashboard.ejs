<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - SIPANDI</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="icon" type="image/png" href="/images/logo-biro-organisasi.jpg">
</head>
<body class="bg-gray-100 font-sans antialiased">
    <div class="flex h-screen overflow-hidden">
        
        <%- include('../partials/sidebar', { page: 'dashboard' }) %>

        <div class="relative flex flex-col flex-1 overflow-y-auto overflow-x-hidden">
            
            <%- include('../partials/header', { pageTitle: 'Dashboard Admin' }) %>

            <main class="w-full flex-grow p-6">
                
                <div class="mb-8">
                    <h2 class="text-3xl font-bold text-gray-800 mb-2">Selamat Datang, <%= user.nama || user.name %>!</h2>
                    <p class="text-gray-600">Anda login sebagai Administrator</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                    <div class="card bg-gradient-to-br from-sipandi-green-500 to-sipandi-green-600 text-white transform hover:scale-105 transition duration-300">
                        <div class="flex justify-between items-start">
                            <div>
                                <h3 class="text-lg font-semibold mb-2 opacity-90">Total Pengguna</h3>
                                <p class="text-4xl font-bold">2</p>
                            </div>
                            <div class="p-2 bg-white bg-opacity-20 rounded-lg">
                                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
                            </div>
                        </div>
                    </div>
                    </div>

                <div class="card bg-white shadow-md rounded-lg p-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4 border-b pb-2">Informasi Sistem</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="space-y-3 text-gray-600">
                            <p class="flex items-center"><span class="font-semibold w-24">Nama:</span> <%= user.nama || user.name %></p>
                            <p class="flex items-center"><span class="font-semibold w-24">NIP:</span> <%= user.nip || '-' %></p>
                        </div>
                        <div class="space-y-3 text-gray-600">
                            <p class="flex items-center"><span class="font-semibold w-24">Role:</span> <span class="px-2 py-1 bg-blue-100 text-blue-800 rounded text-xs uppercase font-bold"><%= user.role %></span></p>
                            <p class="flex items-center"><span class="font-semibold w-24">User ID:</span> #<%= user.id_user || user.id %></p>
                        </div>
                    </div>
                </div>

            </main>

            <%- include('../partials/footer') %>
            
        </div>
    </div>
</body>
</html>